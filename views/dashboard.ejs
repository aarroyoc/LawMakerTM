<%- include('helpers/header', {title: "Centro de trabajo"}); %>

<body>
    <nav class="navbar navbar-expand-md navbar-light bg-light fixed-top">
        <div class="container">
            <a class="navbar-brand" href="/dashboard">Law Maker&trade;</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExampleDefault"
                aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarsExampleDefault">
                <!--
                <ul class="navbar-nav mr-auto">
                    
                    <li class="nav-item active">
                        <a class="nav-link" href="#">Inicio </a>
                    </li>
                </ul>
                <form class="form-inline my-2 my-lg-0">
                    <button type="button" class="btn btn-outline-primary">
                        Iniciar sesión
                    </button>
                </form>
                -->
            </div>
        </div>
    </nav>

    <main role="main" class="container">
        <h1 class="title mt-2 mb-4">Centro de trabajo</h1>
        <a href="/newArticle" type="button" class="mb-2 btn btn-success">Nueva propuesta de artículo</a>
        <nav>
            <div class="nav nav-tabs" id="nav-tab" role="tablist">
                <a class="nav-item nav-link active" id="nav-need-work-tab" data-toggle="tab" href="#nav-need-work"
                    role="tab" aria-controls="nav-need-work" aria-selected="true">Abiertos</a>
                <a class="nav-item nav-link" id="nav-proposed-tab" data-toggle="tab" href="#nav-proposed" role="tab"
                    aria-controls="nav-proposed" aria-selected="false">Propuestos</a>
                <a class="nav-item nav-link" id="nav-closed-tab" data-toggle="tab" href="#nav-closed" role="tab"
                    aria-controls="nav-closed" aria-selected="false">Cerrados</a>
                <a class="nav-item nav-link" id="nav-starred-tab" data-toggle="tab" href="#nav-starred" role="tab"
                    aria-controls="nav-starred" aria-selected="false">En seguimiento</a>
                <a class="nav-item nav-link" id="nav-amendment-tab" data-toggle="tab" href="#nav-amendment" role="tab"
                    aria-controls="nav-amendment" aria-selected="false">Enmiendas</a>
            </div>
        </nav>
        <div class="tab-content mt-3" id="nav-tabContent">
            <div class="tab-pane fade show active" id="nav-need-work" role="tabpanel"
                aria-labelledby="nav-need-work-tab">
                <table id="proposed" class="table table-striped table-bordered" style="width:100%">
                    <thead>
                        <tr>
                            <th>Art.</th>
                            <th>Título</th>
                            <th>Etiquetas</th>
                            <th>Seguimiento</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for(let article of openArticles){ %>
                        <tr>
                            <td> <a href="/article/view/<%= article.id %>"><%= article.n_article %></a></td>
                            <td><%= article.title %></td>
                            <td>
                                <% for(let tag of article.tags) { %>
                                <a href="#" class="badge badge-warning">#<%= tag %></a>
                                <% } %>
                            </td>
                            <td>
                                <% if(article.starred_by.indexOf(mail) > -1) { %>
                                <a href="/notifyTo/<%= article.id %>/unfollow" type="button"
                                    class="btn btn-warning">Dejar de seguir</a>
                                <% } else {  %>
                                <a href="/notifyTo/<%= article.id %>/follow" type="button"
                                    class="btn btn-success">Seguir</a>
                                <% } %>
                            </td>
                        </tr>
                        <% } %>
                    </tbody>
                    <tfoot>
                        <tr>
                            <th>Art.</th>
                            <th>Título</th>
                            <th>Etiquetas</th>
                            <th>Seguimiento</th>
                        </tr>
                    </tfoot>
                </table>
            </div>
            <div class="tab-pane fade" id="nav-proposed" role="tabpanel" aria-labelledby="nav-proposed-tab">
                <table id="proposed" class="table table-striped table-bordered" style="width:100%">
                    <thead>
                        <tr>
                            <th>Art.</th>
                            <th>Título</th>
                            <th>Etiquetas</th>
                            <th>Seguimiento</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for(let article of proposedArticles){ %>
                        <tr>
                            <td> <a href="/article/view/<%= article.id %>"><%= article.n_article %></a></td>
                            <td><%= article.title %></td>
                            <td>
                                <% for(let tag of article.tags) { %>
                                <a href="#" class="badge badge-warning">#<%= tag %></a>
                                <% } %>
                            </td>
                            <td>
                                <% if(article.starred_by.indexOf(mail) > -1) { %>
                                <a href="/notifyTo/<%= article.id %>/unfollow" type="button"
                                    class="btn btn-warning">Dejar de seguir</a>
                                <% } else {  %>
                                <a href="/notifyTo/<%= article.id %>/follow" type="button"
                                    class="btn btn-success">Seguir</a>
                                <% } %>
                            </td>
                        </tr>
                        <% } %>
                    </tbody>
                    <tfoot>
                        <tr>
                            <th>Art.</th>
                            <th>Título</th>
                            <th>Etiquetas</th>
                            <th>Seguimiento</th>
                        </tr>
                    </tfoot>
                </table>
            </div>
            <div class="tab-pane fade" id="nav-closed" role="tabpanel" aria-labelledby="nav-closed-tab">
                <table id="closed" class="table table-striped table-bordered" style="width:100%">
                    <thead>
                        <tr>
                            <th>Art.</th>
                            <th>Título</th>
                            <th>Etiquetas</th>
                            <th>Seguimiento</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for(let article of closedArticles){ %>
                        <tr>
                            <td> <a href="/article/view/<%= article.id %>"><%= article.n_article %></a></td>
                            <td><%= article.title %></td>
                            <td>
                                <% for(let tag of article.tags) { %>
                                <a href="#" class="badge badge-warning">#<%= tag %></a>
                                <% } %>
                            </td>
                            <td>
                                <% if(article.starred_by.indexOf(mail) > -1) { %>
                                <a href="/notifyTo/<%= article.id %>/unfollow" type="button"
                                    class="btn btn-warning">Dejar de seguir</a>
                                <% } else {  %>
                                <a href="/notifyTo/<%= article.id %>/follow" type="button"
                                    class="btn btn-success">Seguir</a>
                                <% } %>
                            </td>
                        </tr>
                        <% } %>
                    </tbody>
                    <tfoot>
                        <tr>
                            <th>Art.</th>
                            <th>Título</th>
                            <th>Etiquetas</th>
                            <th>Seguimiento</th>
                        </tr>
                    </tfoot>
                </table>
            </div>

            <div class="tab-pane fade" id="nav-starred" role="tabpanel" aria-labelledby="nav-starred-tab">
                <table id="closed" class="table table-striped table-bordered" style="width:100%">
                    <thead>
                        <tr>
                            <th>Art.</th>
                            <th>Título</th>
                            <th>Etiquetas</th>
                            <th>Seguimiento</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for(let article of starredArticles){ %>
                        <tr>
                            <td> <a href="/article/view/<%= article.id %>"><%= article.n_article %></a></td>
                            <td><%= article.title %></td>
                            <td>
                                <% for(let tag of article.tags) { %>
                                <a href="#" class="badge badge-warning">#<%= tag %></a>
                                <% } %>
                            </td>
                            <td>
                                <% if(article.starred_by.indexOf(mail) > -1) { %>
                                <a href="/notifyTo/<%= article.id %>/unfollow" type="button"
                                    class="btn btn-warning">Dejar de seguir</a>
                                <% } else {  %>
                                <a href="/notifyTo/<%= article.id %>/follow" type="button"
                                    class="btn btn-success">Seguir</a>
                                <% } %>
                            </td>
                        </tr>
                        <% } %>
                    </tbody>
                    <tfoot>
                        <tr>
                            <th>Art.</th>
                            <th>Título</th>
                            <th>Etiquetas</th>
                            <th>Seguimiento</th>
                        </tr>
                    </tfoot>
                </table>
            </div>

            <div class="tab-pane fade" id="nav-amendment" role="tabpanel" aria-labelledby="nav-amendment-tab">
                    <table id="closed" class="table table-striped table-bordered" style="width:100%">
                        <thead>
                            <tr>
                                <th>Art.</th>
                                <th>Título</th>
                                <th>Etiquetas</th>
                                <th>Seguimiento</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% for(let article of amendmentArticles){ %>
                            <tr>
                                <td> <a href="/article/view/<%= article.id %>"><%= article.n_article %></a></td>
                                <td><%= article.title %></td>
                                <td>
                                    <% for(let tag of article.tags) { %>
                                    <a href="#" class="badge badge-warning">#<%= tag %></a>
                                    <% } %>
                                </td>
                                <td>
                                    <% if(article.amendment_by.indexOf(mail) > -1) { %>
                                    <a href="/notifyTo/<%= article.id %>/unfollow" type="button"
                                        class="btn btn-warning">Dejar de seguir</a>
                                    <% } else {  %>
                                    <a href="/notifyTo/<%= article.id %>/follow" type="button"
                                        class="btn btn-success">Seguir</a>
                                    <% } %>
                                </td>
                            </tr>
                            <% } %>
                        </tbody>
                        <tfoot>
                            <tr>
                                <th>Art.</th>
                                <th>Título</th>
                                <th>Etiquetas</th>
                                <th>Seguimiento</th>
                            </tr>
                        </tfoot>
                    </table>
                </div>
        </div>
    </main>

    <!-- /.container -->

    <%- include('helpers/footer'); %>